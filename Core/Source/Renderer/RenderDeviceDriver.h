#pragma once

namespace Dodo {

    ////////////////////////////////////////////////////////////////
    // RENDER DEVICE DRIVER TYPE ///////////////////////////////////
    ////////////////////////////////////////////////////////////////

    enum class RenderDeviceDriverType
    {
        Vulkan,
        AutoCount,
        None
    };

    ////////////////////////////////////////////////////////////////
    // RENDER DEVICE DRIVER ////////////////////////////////////////
    ////////////////////////////////////////////////////////////////

    class RenderDeviceDriver : public RefCounted
    {
    public:

        virtual ~RenderDeviceDriver() = default;

        virtual void Destroy() = 0;
    };

}